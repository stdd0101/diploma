version: '3'
services:
  test-minio:
    image: minio/minio
    command: minio server /data
    ports:
      - 9990:9990
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123
